program factorial_rec;

fn fact(n: Int): Int; {
    if (n = 0) then
        fact := 1
    else
        fact := n * fact(n - 1);
};

var input: Int;
{
    input := readln();
    writeln(fact(input));
}.
