program optimisation;

var I: Int;
{
	{
		{
			{
				{
					{
						I := (1 + 2) * 3;
						writeln(I);
					}
				}
			}
		}
	};
	{
		{
			I := I + real_to_int(int_to_real((17 and 16)) * 16#100.0);
		}
	};
	I := I * 1;
	writeln(I);

	if (FALSE) then {
		writeln(67);
	};

	for I := 0 to 10 * 2 do {
		writeln(I);
		break;
		writeln(67);
	}
}.
